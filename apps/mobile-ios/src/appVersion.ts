// Single source of truth for the user-visible app version.
// Bump this in lockstep with android/app/build.gradle versionName + package.json.
// (Keeps Settings, Help, and every other screen from drifting apart.)
export const APP_VERSION = '1.9.0';
