json_key_file(ENV["SUPPLY_JSON_KEY_PATH"] || "")
package_name("com.ankr.shield")

# iOS
app_identifier("com.ankr.shield")
apple_id(ENV["APPLE_ID"] || "")
itc_team_id(ENV["ITC_TEAM_ID"] || "")
team_id(ENV["APPLE_TEAM_ID"] || "")
